# DTOについて

## DTOを導入するきっかけ
DTOを導入する前は、ControllerでEntityをそのまま返していた。
これだと、DBの情報がすべてAPIに出てしまう。
実際のシステムではユーザーには見せたくない情報があり、APIとEntityは別物として考える必要がある。
これらの観点から、Entityをそのまま返すのは適切ではないと分かり、入力用DTOと出力用DTOを作ることにした。

